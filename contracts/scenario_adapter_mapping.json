{
  "schema_version": "scenario-adapter-mapping-v0",
  "role": "Evaluator-visible mapping from compiled Scenario JSON fields to Mesa model constructor inputs",
  "model_families": {
    "aircraft_support_v1": {
      "model_class": "AircraftSupportV1Model",
      "simulation_inputs": {
        "schema_version": {
          "source": "aircraft_support_v1 input schema contract",
          "status": "metadata_only",
          "reason": "The schema version selects the governed input contract and is not a model constructor parameter."
        },
        "project_identity": {
          "source": "Project JSON identity envelope and Scenario compiler provenance",
          "status": "governance_only",
          "reason": "Identity fields trace the compiled platform chain and do not change simulation dynamics."
        },
        "mission_profile": {
          "source": "project.missionProfile, project.basicMissions including missionPhases, legacy project.missionPhases fallback, and project.airports",
          "constructor_param": "mission_profile"
        },
        "aircraft": {
          "source": "project.equipment.quantity, project.equipment.initialReady, project.equipment.wholeMachineModels",
          "constructor_param": "aircraft"
        },
        "equipment_tree": {
          "source": "project.components[] hierarchy with failureDistribution, kOutOfN, productType, quantity, and specialRepairProfile",
          "constructor_param": "equipment_tree"
        },
        "support_network": {
          "source": "project.supportNodes[] plus canonical project.supportOrganization, supportNodes[].organizationNodeId, supportResources[].organizationNodeId, and project.transportPolicies[] organization endpoints",
          "constructor_param": "support_network",
          "behavior": "organization_graph.runtime_mode is persisted from project.supportOrganization.runtimeMode. vertical_lateral uses local first, then enabled direct incoming lateral relations ordered by relation priority/id/source, then the #315 parent chain; vertical preserves #315 local/parent behavior and legacy preserves support-node behavior. Missing runtime_mode falls back to vertical for a non-empty graph and legacy for an empty graph."
        },
        "support_activities": {
          "source": "project.supportActivityJobs[] definitions plus project.supportActivities[] activityCodes/predecessors DAG",
          "constructor_param": "support_activities"
        },
        "time": {
          "source": "project.missionProfile.durationHours and M9.7 default tick/sample policy",
          "constructor_param": "time_config"
        },
        "monte_carlo": {
          "source": "project.experiment.samples and formal ExperimentPlan.config.analysisRequests.largeSample sweep via MonteCarloRunConfig; persisted Project sweep arrays are stripped",
          "constructor_param": "monte_carlo_config"
        },
        "seed": {
          "source": "project.experiment.seed",
          "constructor_param": "seed"
        },
        "stop_policy": {
          "source": "ExperimentPlan.config.stopPolicy, defaulted to duration stop when absent",
          "constructor_param": "stop_policy"
        }
      }
    }
  }
}
