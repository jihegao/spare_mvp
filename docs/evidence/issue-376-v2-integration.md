# Issue #376：在真实 V2 基线上集成并发布

## 权威基线和集成范围

V2 应用基线为 `e0f7824ecc6d9d2d00543aafd978e70b35bedfc4`，验收文档提交为 `f0a003d717238a7c87df570445e86b8774ccbb70`。它包含逐样本可靠度、三阶段日保障、原子即时备件满足率、标准 Excel 无损导入和 Windows 完整离线运行能力。基线已保存为远端 `codex/v2-accepted-baseline`。

旧 main `0a3dd80` 与该 V2 共同祖先为 `1091c98`，分别独有 1 与 34 个提交。此前 #366 九 PR 和 #367 性能证据均在旧 main 线上，不能代表完整 V2。本次从 V2 创建 `dev`，以保留祖先关系的 merge 整合 #368、#369、#370、#371、#372、#373、#374、#375、#377，不用文件覆盖吞掉 V2 实现。#365 的早期性能实现已在 V2 历史中保留，不重复叠加另一套算法。

移除产品“运行五项分析”的按钮、状态、执行器、专属模块和测试；五个原有功能入口保留。外部验收工具逐页点击原按钮，新建当前 F35 的冻结方案，使用权威50样本/8workers参数，每页独立请求和新session；禁止修改请求或注入产品状态。旧历史测量保留并标为非V2，不能替代新验收。

## 语义和回归

- 单一数据分页保留 V2 逐样本可靠度明细、全量趋势/导出、完整携行列及预计/即时满足率区别。
- 编译运行时仍继承 V2 的规范模型。差分覆盖日保障三阶段、空阶段、跨午夜、资源受限和样本模板隔离；额外验证本地/远程补货的非零即时满足率，比较完整业务输出、终态和随机态。仅排除旧优化已明确允许变化的重复等待诊断计数。
- demo JS 同步权威 public canonical 中的5个机型适用范围字段；不改 Project 编译规则。独立深比较及导入测试通过。
- V2 Excel 使用保真二进制 OOXML writer；旧 HTTP 故障注入 mock 的 Workbook.save 已不再执行。测试改向真实 workbook_bytes 边界注入并断言调用，原400错误码/提示、成功文件/权限/UTF8检查均保留，生产代码未因此改变。
- 本地 npm 全量721项通过；后续工具故障处理5项和前端针对性9项通过；浏览器7项覆盖分页、104行全量导出、导航、项目切换和autosave重试。编译执行器既有39项及新增11个V2差分场景通过。完整后端707项运行结束：旧Excel mock失败及未stage删除文件导致的打包索引错误均已定位；修复/stage后，打包、Excel全相关suite、HTTP故障契约及V2差分共48项复测全部通过，其余完整运行无失败。

## 目标环境和发布边界

2026-09-18现场确认主机4700-4，目标现有V2包位于 `C:\Users\user\Models\spare_mvp-acceptance-20260918T151500-1091c98\package-e0f7824`。关键应用文件与相邻 source-e0f7824 哈希一致；另外的 compiled 目录缺失V2 operations_engine，不能作为V2源。

目标锁定运行时为Python3.13.15（packaging/windows-runtime.json），CPU Ryzen7 4700U/8逻辑线程，RAM 7,962,664,960 bytes。保留既有已验证离线依赖和资源；Linux3.12本地数字不作为目标机验收。

新隔离目录为 `C:\Users\user\Models\spare_mvp-v2-release-20260918-issue376`。原代码、manifest及SQLite只读backup已保留，数据库quick_check为ok；旧服务4173/8767仍运行。候选使用独立端口验证后才合并main并按main SHA构建发布；原包及其运行库保留作回滚，不修改4700-3。

## 4700-4 候选五原页面验收

候选源码 `ea1b704d5d19ecc7977c9ee84b27131d13ee06d0`，包内 source manifest 绑定该提交；18,824 个不可变文件校验通过，555 个前端离线资源和90个离线wheel通过构建校验。原生 Windows Edge153.0.4234.32、Python3.13.15，在2026-09-18 23:54:54至23:59:47（Asia/Shanghai，UTC15:54:54至15:59:47）执行。后续发布跨至9月19日，目录日期表示本次流程启动日。

| 原有入口 | 点击至完整显示（秒） | 完成/失败 | workers |
|---|---:|---:|---:|
| Monte Carlo | 54.688 | 50/0 | 8 |
| 备件短板 | 55.472 | 50/0 | 8 |
| 转场携行 | 55.840 | 50/0 | 8 |
| 任务可靠度 | 55.613 | 50/0 | 8 |
| 停机因素 | 57.037 | 50/0 | 8 |

五个原按钮分别发起新请求、五个独立session，响应HTTP200/session_complete；未复用一组样本、未改请求设置。计时源为浏览器performance.now，从真实click到当前方案结果非占位DOM渲染后双requestAnimationFrame；完整API采集与身份核验另行等待，不用后端秒数冒充前端时间。CDP配置256MiB总缓冲/128MiB单资源以完整采集最大13.5MB响应，不保存原始payload或trace。预检零分析请求、五页原入口可用且无产品批量按钮。首次工具等待已隐藏autosave成功文案失败时没有启动分析，修为权威Project GET完整响应和身份校验后正式重跑，不能把预检失败当业务失败或省略正式结果。

冻结方案由当前权威Project通过UI新建并冻结，50样本、8workers、seed20260621。只读后验重新通过唯一SimulationAdapter编译，确认与当前Project相同input hash `ad13e1afd88f3f90fb430e2aa3f1da33c0ab0d0e0c692baee1d89950eb61d532`；实际9架飞机、423行为组件、43天、104波次。此前“8架飞机”的描述不正确，本次9飞机与8workers分别记录；未缩减案例或场景。所有响应的project、冻结指纹、scenario/version与该输入一致。

资源观测覆盖准备和正式串行运行：CPU峰值100%，候选Python进程合计峰值RSS1,180,692,480 bytes，最低系统可用内存1,181,650,944 bytes。进程数峰值11含后端、Solara、worker和监控，不等同worker数；正常分析仍8workers。旧4173/8767服务保持空闲运行，未为了计时停用户服务。没有并发其他仿真或全量测试。

聚合证据见 `issue-376-v2-target-candidate.json`。工具hydration/readiness和身份校验修改经独立终审、5项工具测试通过；这些后续修改不改变已测试候选生产应用。最终main包仍需绑定实际merge SHA并验证生产文件等价及4173/8767实际切换。
