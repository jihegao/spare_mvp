# Issue #366 集成验收

本次八个实现 PR 在独立基线 `codex/issue-366-integration` 汇总，未合并到 `main`。验收代码 SHA：`dd6d88a47a3b709b4ef436a15c18c9496e9077d2`。

| 工作包 | PR | head | base |
| --- | --- | --- | --- |
| 界面布局 | #368 | 7d0ac12 | main |
| 装备树及周期状态 | #370 | d9ac67e | main |
| 实验权限 | #371 | bb55bf0 | main |
| 创建性能 | #373 | 735c3e3 | main |
| 保障视图排序 | #369 | d3c4409 | main |
| 分页基础 | #372 | fb6f3f5 | main |
| 建模分页 | #375 | fa25730 | codex/issue-366-pagination-core |
| 分析分页 | #374 | 0079420 | codex/issue-366-pagination-core |

该验收 PR 以集成分支为 base，仅承载脚本和记录；不能直接视为可合并到 main。须先按依赖合并八个实现 PR，确认最终代码一致，再将验收 PR retarget 到 main 并核查差异。

## 环境与数据保护

- Linux 6.8；AMD Ryzen Threadripper PRO 3995WX；128逻辑CPU，约125.6 GiB RAM。
- 显式使用仓库 `.abm-mesa-test-env/bin/python`：Python 3.12.13、Mesa 3.5.1、Solara 1.57.5、jsonschema 4.26.0、NumPy 2.4.6。
- 当前本机只读 SQLite 中的 F35 案例，通过 SQLite backup 创建在权限700临时目录的副本。后端、浏览器和任何新增实验只访问副本，私有数据库、原始案例、原始分析响应均不提交。
- 两个仅本次拥有的服务端口4189/4190，前者为候选，后者为main基线；性能实验串行，运行时暂停本任务测试与其他仿真。
- 早期系统Python3.10诊断结果已废弃为正式验收依据。最终记录仅采用上述锁定环境。

## 第 1–12 项覆盖与边界

| 条目 | 已有覆盖 | 验收边界 |
| --- | --- | --- |
| 1、4、5 | 登录布局、模块名称单行及 MTBF/MTTR 控件样式已实现，随前端契约和运行时回归检查。 | 自动化通过不等于所有分辨率的视觉检查完成。 |
| 2 | 建模和分析表格复用每页 20 条分页；覆盖 0/20/21/41 条、筛选排序、页码调整、当前页选择、重新运行及全量导出；分析快照每页首条展开。 | 树、固定月份格、图表及指标卡不分页；不将分页后的可见行数当作完整业务结果。 |
| 3、6 | 装备树拖动、键盘、缩窗后立即调整与重渲染保持已有浏览器回归；年配置覆盖 0/1/12 月、保存重载和非法引用。 | 年状态“已配置”只表示至少引用一个有效月剖面，不能替代编译有效性判断。 |
| 7、8 | 所有者删除、跨用户拒绝、历史无所有者记录及各角色取消冻结有 API/UI 回归；取消冻结清除选中上下文和旧结果，并拒绝迟到结果；未知方案删除返回结构化错误。 | 保留既有管理员权限和项目访问边界，未新增跨项目授权。 |
| 9 | 本机同案例五轮真实浏览器新增、保存、预检及落库对照，见下文。 | 创建性能没有套用第 13 项的 60 秒阈值。 |
| 10 | Solara 主按钮样式改为与“运行推演”一致的蓝色。 | **真实 Solara 页面按钮颜色尚未验收**，不能以样式实现或组件测试代替。 |
| 11 | 库存、已消耗、在途支持数值升降序、稳定同值排序及帧刷新保持；实际 Solara render/click 回归所在测试文件 33 项通过。 | 此处是组件渲染与事件验证，不是目标 Windows 主机上的真实页面验收。 |
| 12 | 方案选项保持名称显示，新增同名方案按不同稳定 ID 构建请求的回归。 | 名称不充当方案身份。 |

最终集成检查中，`npm test` **710 项通过**，Python 后端增量回归 **15 项通过**。此前 Exporter、Project contract、Simulation Adapter 与 canonical input fixtures 的 **136 项检查通过**；Solara 的 33 项是前述组件验证轮次，分别记录，不替代最终环境或逐页面业务验收。

## 创建性能

锁定环境下，同一F35初始数据库副本，main `0a3dd80` 与候选代码 `3d8124c` 各五轮真实浏览器“新增→保存→API预检→列表显示”。该候选与最终集成的创建优化代码一致；后续只修复其他交互和补充记录。

完整链路中位3094.24→2539.69ms，改善约17.9%；预检1585.12→923.83ms。两组各五次HTTP200并真实落库。详见同目录 `issue-366-experiment-creation.md`。

## 可复现浏览器验收

`scripts/issue-366-browser-acceptance.mjs` 连接已运行的隔离服务，不启动或修改用户运行服务。通过环境变量指定 `BASE_URL`、`ACCEPTANCE_USER`、`ACCEPTANCE_PASSWORD`、`TESTED_SHA` 和仓库外绝对路径 `RESULT_DIR`；凭据不输出。默认仅选择唯一F35项目，其他项目必须用 `PROJECT_NAME` 显式指定。

脚本等待Project加载完成，执行五次各50样本、8workers的真实请求，校验独立session、零执行失败、相同输入身份和UI显示计时，再逐一进入四个分析页核实50样本、真实数据行、当前来源和可导出状态。输出仅含白名单元数据及身份hash；可选截图也只能保存在仓库外目录。

目标4700-4的Windows版本未部署或复验，本机结果不能代替该目标环境验收。第13项只有目标环境页面计时通过后才能最终勾选；本次不关闭#366。

## 最终本机运行证据

在上述冻结SHA和锁定解释器下，最终浏览器验收 PASS。每项均为真实50样本、8workers、HTTP200、50完成/0失败；五个不同session，Project/context及输入hash一致。

| 项目 | 浏览器结果显示计时（秒） | 后端（秒） |
| --- | ---: | ---: |
| Monte Carlo | 22.80 | 22.18 |
| 备件短板 | 22.93 | 22.35 |
| 转场携行 | 22.62 | 22.04 |
| 任务可靠度 | 23.08 | 22.47 |
| 停机因素 | 24.28 | 22.87 |

随后四个独立分析页均显示50样本、当前Project来源、20条真实数据行及可用导出按钮；停机页另显示4个快照。页面导航没有新增分析请求。分页翻页/筛选等功能由前述针对性测试覆盖，此处DOM计数不替代完整分页功能验收。

完整白名单元数据见 `issue-366-browser-acceptance.json`。首轮五项业务成功但最后大响应采集失败，脚本按失败退出；修复CDP采集后完整重跑，本表仅引用最终一轮。被动采集使用同一CDP session的256MiB总/128MiB单响应有界内存缓冲，未启用trace或持久化响应正文；最大响应约12.8MB。此采集开销包含于本轮浏览器观测中。

## 收尾与独立复核

独立评审复核最终脚本和白名单报告，无阻塞问题。导出按钮可用不等于本轮重新验证导出文件内容；四页DOM计数不等于重复全部交互测试。

原SQLite文件运行前后SHA-256一致；仅停止本次拥有的两个服务及其子进程，4189/4190监听已释放。验收浏览器在脚本finally中关闭，私有数据库副本、分析输出和临时目录已删除，临时node_modules软链接已移除。仅保留上述白名单报告和不含私有数据的测试证据。原始工作区保持clean，未合并PR或改动目标4700-4服务。
