# 20260627 TODO

## 1. 系统运行支持模块/项目管理/项目数据管理

- [ ] 分为两个模块进行配置
- [ ] 去掉最上面仿真建模数据表 sheet 选择器
- [ ] 所有系统运行支持模块可下一阶段再进行修改

## 2. 系统运行支持模块/项目管理/建模颗粒度管理

- [ ] 预设两种颗粒度：颗粒度 A（包含全部表单字段）、颗粒度 B

## 3. 系统运行支持模块/装备 RMS 指标分配

- [ ] 反算的字段改名
- [ ] 节点分配结果去掉暴露时间、R 目标字段
- [ ] 输入数据增加运行比（显示在左侧结构树对应节点，通过运行比计算产品强度）
- [ ] 新增任务时长配置。任务可靠度指标按 MTBCF 建模，系统根据任务可靠度和任务时长计算 MTBCF；在配置关键故障占比或故障类型映射关系后，进一步折算输出 MTBF。

## 4. 系统运行支持模块/系统基础配置/用户管理

## 5. 系统运行支持模块/系统基础配置/系统功能权限管理

- [ ] 配置三类用户对各模块/表格的权限：`只读` 或 `编辑`

## 6. 系统运行支持模块/系统基础配置/建模表单管理

- [ ] 进行字段建模单位管理（时间相关：小时/分钟）
- [ ] 保障人员专业字典维护

## 7. 备件规划评估模块/装备系统建模/装备系统建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 支持左侧的装备结构树导入
- [ ] MTBF、MTTR 分布类型只有固定值、指数、正态、均匀分布四种，其他去掉
- [ ] 选固定值，只有一个分布相关参数固定值；选指数，只有均值；选正态，只有均值和标准差；选均匀，只有最大值、最小值
- [ ] MTBF、MTTR 选好分布后，分布相关参数显示不齐
- [ ] 示例数据：舰载机节点删掉
- [ ] 组件属性如果是 SRU，上级必须有 LRU；空值改为空格
- [ ] 左边为装备组成树，如果点飞机级，右边表格按节点从高往低顺序进行排序；点系统，右边表格显示该节点以及以下子节点的列表

## 8. 任务可靠度评估模块/装备系统建模/装备可靠性框图建模

- [ ] 左边装备树，点到整机，显示下一级的节点，有门的另外处理

## 9. 备件规划评估模块/装备任务建模/基本任务建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 去掉返回时间比字段
- [ ] 任务阶段模块放到上面基本任务信息编辑的框里
- [ ] 增加提前通知时间字段

## 10. 备件规划评估模块/装备任务建模/复合任务建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 典型组合任务时序表波次序号不连续，应该按照出动时刻给组合后的任务进行排序

## 11. 备件规划评估模块/装备任务建模/周期性任务建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 周期性任务建模列表每条任务的选择/删除按钮去掉，在新增按钮右侧增加删除按钮，点击删除按钮，将该任务从列表中删除
- [ ] 周期性任务建模上一级再增加一层任务，能够在每周周内配置不同的复合任务

## 12. 备件规划评估模块/装备任务建模/基本作战单元建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 日历日时间逻辑，理解为大修周期

## 13. 备件规划评估模块/保障组织建模/保障组织结构建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 目前左侧结构树只能建到 3 级，应该支持一直往下层节点建模

## 14. 备件规划评估模块/保障组织建模/备件建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 增加所属装备字段
- [ ] 去掉编辑按钮

## 15. 备件规划评估模块/保障组织建模/保障人员建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 专业字段改为下拉选择形式（专业字典在建模表单里维护）
- [ ] 去掉所属型号字段
- [ ] 去掉编辑按钮

## 16. 备件规划评估模块/保障组织建模/保障设备建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 去掉编辑按钮

## 17. 备件规划评估模块/保障活动建模/基本保障活动建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 去掉即改即存形式，在编辑按钮里修改
- [ ] 点击编辑弹出面板，对保障人员、保障设备、备件需求进行编辑（参考海航大对应模块原型）
- [ ] 模块需要支持：
  - [ ] 基本保障活动列表查询；
  - [ ] 新增、编辑、删除基本保障活动；
  - [ ] 按活动类型导入 Excel；
  - [ ] 维护活动基础信息，包括活动编号、工作名称、适用对象（飞机）、作业时长、保障人员、保障设备、备件需求等；
  - [ ] 作业时长可配置分布类型，包括：固定值、指数分布、正态分布、均匀分布

## 18. 备件规划评估模块/保障活动建模/使用保障活动建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 方案名称放三个阶段的上面，三个阶段都是同一方案名称
- [ ] 去掉最大时间参考字段
- [ ] 工作项目清单（参考海航大对应模块原型）：
  - [ ] 新增工作项目-作业项从基本保障活动里选（可选择可搜索），其他字段从选中的基本保障活动后自动填入
  - [ ] 紧前作业字段右侧增加编辑紧前作业按钮，可从基本保障活动建模里选择/搜索基本保障活动

## 19. 备件规划评估模块/保障活动建模/预防性维修活动建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 去掉最大修复时间字段
- [ ] 工作项目清单：
  - [ ] 新增工作项目-作业项从基本保障活动里选（可选择可搜索），其他字段从选中的基本保障活动后自动填入
  - [ ] 紧前作业字段右侧增加编辑紧前作业按钮，可从基本保障活动建模里选择/搜索基本保障活动

## 20. 备件规划评估模块/保障活动建模/修复性维修活动建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 去掉最大修复时间读取装备系统建模的 MTTR，为只读权限
- [ ] 去掉维修对象字段
- [ ] 工作项目清单：
  - [ ] 新增工作项目-作业项从基本保障活动里选（可选择可搜索），其他字段从选中的基本保障活动后自动填入
  - [ ] 紧前作业字段右侧增加编辑紧前作业按钮，可从基本保障活动建模里选择/搜索基本保障活动

## 21. 备件规划评估模块/保障活动建模/后勤保障活动建模（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 方案名称放三个阶段的上面，三个阶段都是同一方案名称
- [ ] 去掉最大时间参考字段

## 22. 备件规划评估模块/仿真实验/仿真实验方案管理（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 去掉场景字典
- [ ] 不要方案列表和方案编辑两个标签页，可以在方案列表里点击编辑按钮跳转编辑页
- [ ] 去掉启动可视化推演、创建蒙特卡洛实验按钮

## 23. 备件规划评估模块/仿真实验/可视化推演（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 处于维修/保障的飞机分开统计
- [ ] 单机状态去掉最上面的飞机选择，只在最左侧选择单机

## 24. 备件规划评估模块/仿真实验/蒙特卡洛实验（任务可靠度评估模块：与备件规划评估模块一致）

- [ ] 完成集成

## 25. 备件规划评估模块/结果分析/蒙特卡洛实验结果

- [ ] 去掉该模块，结果返回在蒙特卡洛实验页面

## 26. 备件规划评估模块/结果分析/备件短板分析

- [ ] 可配置备件满足率 0.85/0.9/0.95、备件利用率 0.85/0.9/0.95 的约束条件
- [ ] 对结果进行截断
- [ ] 完成集成

## 27. 备件规划评估模块/结果分析/飞机转场携行清单

- [ ] 去掉优化条件，默认携带的越少越好
- [ ] 完成集成

## 28. 任务可靠度评估模块/结果分析/蒙特卡洛实验结果

- [ ] 去掉该模块，结果返回在蒙特卡洛实验页面

## 29. 任务可靠度评估模块/结果分析/飞机任务可靠度分析

- [ ] 去掉该模块

## 30. 任务可靠度评估模块/结果分析/任务可靠度评估模块

- [ ] 折线图坐标轴纵坐标最下面为 0
- [ ] 具体需求待甲方确定

## 31. 任务可靠度评估模块/结果分析/停机因素分析

- [ ] 记录出现停机的异常情况，进行快照记录
- [ ] 记录时间点、发生的事件、结果
- [ ] 模块需支持：
  - [ ] 自动识别异常事件；
  - [ ] 在异常发生时记录异常时刻；
  - [ ] 保存异常发生时的保障活动状态快照；
  - [ ] 支持查看异常快照详情；
  - [ ] 支持从异常快照定位到对应作业节点；
  - [ ] 支持导出异常快照记录；
  - [ ] 支持删除异常快照记录。
- [ ] 完成集成
